<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Set Fee Plan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  </head>
  <body class="bg-light">
    <div class="container py-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h3 m-0">Set Active Fee Plan</h1>
        <a class="btn btn-outline-secondary" href="/dashboard">Back to Dashboard</a>
      </div>

      <%- include("./partials/alerts.ejs", { info: typeof info !== 'undefined' ? info : [], success: typeof success !== 'undefined' ? success : [], error: typeof error !== 'undefined' ? error : [] }) %>

      <% if (feePlan) { %>
        <div class="alert alert-info" role="alert">
          <div class="d-flex justify-content-between">
            <div>
              <strong>Current Plan:</strong> <%= feePlan.title %> — ₹ <%= Number(feePlan.amount).toLocaleString() %>
            </div>
            <small class="text-muted">Updated: <%= formatDate(feePlan.updatedAt || feePlan.createdAt) %></small>
          </div>
        </div>
      <% } else { %>
        <div class="alert alert-warning" role="alert">
          No active fee plan is set. Students cannot pay until you create one below.
        </div>
      <% } %>

      <div class="card shadow-sm">
        <div class="card-body">
          <form action="/fees/plan" method="post">
            <div class="mb-3">
              <label class="form-label">Title</label>
              <input type="text" name="title" class="form-control" placeholder="e.g. Semester Fees 2025" required />
            </div>
            <div class="mb-3">
              <label class="form-label">Amount</label>
              <input type="number" name="amount" min="0" step="0.01" class="form-control" placeholder="e.g. 15000" required />
            </div>
            <button type="submit" class="btn btn-primary">Save Plan</button>
          </form>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
  </html>
